# basic-informational-site

This is the first project in the Node.js portion of The Odin Project. This one allowed me to serve html files based on the URL the client requested. I used: http, fs (file system) and the url modules to make this happen!

The latest commit rewrote the code using the Express.js framework and the environment variable: PORT is the port you would like the Express server to run on
